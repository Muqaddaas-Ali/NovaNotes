# NovaNotes
A clean, minimal note-taking web app built with vanilla HTML, CSS, and JavaScript. Create, search, color-tag, and delete notes — all persisted locally in the browser via localStorage. No frameworks, no dependencies.
